import React from 'react';
import ProfileCard from '@/components/ui/ProfileCard';

const ProfilePage = () => {
    return (
        <div className="h-full flex items-center justify-center px-4 py-4 overflow-hidden">
            <ProfileCard />
        </div>
    );
};

export default ProfilePage;
